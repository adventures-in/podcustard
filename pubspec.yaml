name: podcustard
description: Enjoy your podcasts right here.
publish_to: none

version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  http:
  url_launcher:
  audiofileplayer:
  xml:

  freezed_annotation:
  json_annotation: 

  redfire:
    # git: https://github.com/enspyrco/redfire.git
    path: /Users/nick/git/orgs/enspyrco/redfire

  redux:
  flutter_redux:
  fast_immutable_collections: ^5.0.0-dev.12

  firebase_core: "^1.1.0"
  device_info:

dev_dependencies:
  flutter_test:
    sdk: flutter
  redux_remote_devtools:
  pedantic:
  build_runner:
  freezed:
  json_serializable: 
  test:
  mockito:

flutter:
  uses-material-design: true
